[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "gesture-intent-control"
version = "0.1.0"
description = "Personal hand gesture intent model for desktop control"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]

